# Authority-bounded remote manual demand. Speed is SI m/s; steering is the
# direct normalized front-wheel steering demand in [-1, 1].
builtin_interfaces/Time stamp
bool active
uint64 sequence
uint64 lease_generation
uint64 runtime_epoch
float64 signed_speed_mps
float64 steering_normalized
